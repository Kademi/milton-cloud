*** MVP ***

permissions:
    connect to meta, so is independent of versions

folder sharing:
    from meta
    requires permission

version browsing:
    need file version browsing in each folder, base of metaId
    also should have top level repo version browsing

user profile editing

user registration

html file browser

HA volumes
    - have a static config at first, with a list of VolumeInstances. VI's are assigned
    to a volume as needed, eg if another VI dies.

-------------------------------

contacts

calendar
