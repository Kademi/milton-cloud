Theme css files use LESS to make styles available to app pages via mixins and attributes.
App page css files will explicity use these so they must be defined

Required attributes:
@text1:
@text2:
@hero1:
@hero2:

Required classes:
ul.checkpoints

Required mixins:

 
